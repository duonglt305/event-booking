<span class="dt-length-records">
    <i class="fa fa-globe"></i> <span class="d-none d-sm-inline">{{ trans('table::tables.show_from') }}</span> _START_ {{ trans('table::tables.to') }} _END_ {{ trans('table::tables.in') }} <strong class="bold">_TOTAL_</strong> <span class="hidden-xs">{{ trans('table::tables.records') }}</span>
</span>
